#include <stdio.h>
int main (void)
{
    int x,y,z,rows,a,t;
    printf("how many rows do you want -:\n");
    scanf("%d",&rows);
    for(x=1;x<=rows;++x)
    {
        for (y=1;y<=rows-x;++y)
        {
            printf(" ");
        }
        for (z=1;z<=x;++z)
        {
            if(z<=x) 
            {
                
            }
        }
        printf("\n");
    }
}